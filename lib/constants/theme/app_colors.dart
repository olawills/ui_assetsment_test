import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Singleton

  static const primaryColor = Color(0x0f8f00ff);
  static const whiteColor = Colors.white;
  static const starColor = Color(0xFFFFCC00);
  static const unstar = Color(0xFFF2F2F2);
  static const blackColor = Colors.black;
  static const textColor = Color(0xFF080A24);
  static const cardColor = Color(0XFFEDEDED);
}
